<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <!-- include any necessary CSS or JavaScript files here -->
    <link rel="stylesheet" href="css/style.css" type="text/css">

</head>
<body>
    <header>
        <!-- include your website logo, navigation menu, etc. here -->
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/about">Recipes</a></li>
                <li><a href="/contact">The Team</a></li>
            </ul>
        </nav>
    </header>

    <main>
        {{!-- this is where the content of each page will be inserted --}}
        {{{body}}}
    </main>

    <main>
        <div class="recipe-container">
            {{!-- Loop over your recipe data and render recipe cards --}}
            {{#each recipe}}
                <div class="recipe-card">
                    <img src="{{this.image}}" alt="{{this.name}}">
                    <h3>{{this.name}}</h3>
                    <p>{{this.description}}</p>
                    <!-- Add any additional recipe details as needed -->
                </div>
            {{/each}}
        </div>
    </main>

    <footer>
        <!-- include your footer content here -->
        <p>&copy; 2023 Yumbook</p>
    </footer>
</body>
</html>
